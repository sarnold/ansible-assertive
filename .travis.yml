language: python
python:
    - 2.7
    - 3.6

notifications:
    email: false

env:
  - ANSIBLE_VERSION=2.7.5

before_install:
  - pip install tox
  - git clone --recursive --depth=1 -b $ANSIBLE_VERSION https://github.com/ansible/ansible
  - pip install ./ansible

script:
    - tox
